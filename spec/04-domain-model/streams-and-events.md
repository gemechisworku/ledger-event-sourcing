# Streams & event catalogue

- **Authoritative types:** `ledger/schema/events.py` + `EVENT_REGISTRY` (generator).
- **Catalogue** may evolve — document **gaps** and new events you add.
- **Stream ID formats:** `loan-`, `agent-`, `compliance-`, `audit-` (see aggregates spec / requirements doc).

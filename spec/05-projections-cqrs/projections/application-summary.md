# Projection: ApplicationSummary

**Table fields:** application state, amounts, risk/fraud, compliance summary, agent sessions completed, last event metadata, human reviewer, final decision time.

**Subscriptions:** derive event types from `ledger/schema/events.py`.
